Create a local .env (do not commit)

1. Copy `.env.example` to `.env`
2. Optionally customize settings (like `REGISTRATIONS_PATH`)
3. Optional Supabase (cloud storage): set `SUPABASE_URL` + `SUPABASE_ANON_KEY` (or `SUPABASE_SERVICE_ROLE_KEY`)
4. Keep `.env.example` as placeholders only
